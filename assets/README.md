# Assets

`logo.png` is the Stacey's Little Stars logo image used for the header and hero background.

When replacing it, keep the file name as `logo.png` so the existing HTML and CSS continue to work.
